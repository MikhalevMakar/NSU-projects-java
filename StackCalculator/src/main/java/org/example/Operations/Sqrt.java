package org.example.Operations;
import java.lang.Math;
import org.example.CheckerDoubleValue.CheckerDouble;
public class Sqrt {
    public static double rootCalculation(String value) {
        if(CheckerDouble.IsNumberFormat(value))

    }
     return Math.sqrt(Double.parseDouble(value));
}
